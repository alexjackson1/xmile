pub mod containers;
pub mod core;
pub mod model;

pub mod types;

pub use crate::core::{Identifier, Namespace, Uid};

pub fn add(left: u64, right: u64) -> u64 {
    left + right
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {
        let result = add(2, 2);
        assert_eq!(result, 4);
    }
}
